# og-unfurl-proxy · architecture

Implements the port defined in `contracts/events.ts`.

## Surface

- 4 API endpoints
- 3 events emitted
- 2 jobs declared
- 4 UI surfaces

## Dependencies

Composed via: cloudflare-deploy, notify.

## Hot path

Documented at the port-method level in `contracts/events.ts`. Implementation lives in `src/` (greenfield).

{
  "name": "@multimarcdown/og-unfurl-proxy",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "description": "Cloudflare Worker proxy that sits in front of a host with bot-challenge protection (Hostinger, certain WAFs) and spoofs a desktop Chrome Use",
  "scripts": {
    "build": "tsc -b"
  },
  "dependencies": {
    "@multimarcdown/core": "*",
    "zod": "^3.23.0"
  }
}

{
  "name": "@multimarcdown/performance-loop",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "description": "MultimarcDown capability: performance-loop (L3 MJB 10-metric per-product roll-up + decision-state + feedback routing + cost-per-result + review packets)",
  "scripts": {
    "build": "tsc -b"
  },
  "dependencies": {
    "@multimarcdown/core": "*",
    "@multimarcdown/product-registry": "*",
    "zod": "^3.23.0"
  }
}

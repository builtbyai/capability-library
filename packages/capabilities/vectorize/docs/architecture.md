# vectorize · architecture

Implements the port defined in `contracts/events.ts`.

## Surface

- 5 API endpoints
- 3 events emitted
- 3 jobs declared
- 4 UI surfaces

## Dependencies

Composed via: gpu-router. Secrets resolved via `connector-config`.

## Hot path

Documented at the port-method level in `contracts/events.ts`. Implementation lives in `src/` (greenfield).

{
  "name": "@multimarcdown/webrtc-stream",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "description": "Peer-to-peer audio/video/data streaming via WebRTC. Signals through a Cloudflare Worker; ICE candidate exchange + DTLS-S",
  "scripts": {
    "build": "tsc -b"
  },
  "dependencies": {
    "@multimarcdown/core": "*",
    "zod": "^3.23.0"
  }
}

# widget-framework · architecture

Implements the port defined in `contracts/events.ts`.

## Surface

- 4 API endpoints
- 5 events emitted
- 2 jobs declared
- 8 UI surfaces

## Dependencies

Composed via: (no required capabilities).

## Hot path

Documented at the port-method level in `contracts/events.ts`. Implementation lives in `src/` (greenfield).

{
  "name": "@multimarcdown/windows-control",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "description": "UIA semantic clicking + OCR + template match + window mgmt via the node-a broker pattern (port 990",
  "scripts": {
    "build": "tsc -b"
  },
  "dependencies": {
    "@multimarcdown/core": "*",
    "zod": "^3.23.0"
  }
}

/** claude-code-via-deepseek -- configuration-only workflow.
 *  This recipe ships no runtime wiring; it makes the deepseek-router setup discoverable
 *  from the workflow catalog. Setup steps live in setup.sh / README.md. */
export const CONFIG_ONLY = true;

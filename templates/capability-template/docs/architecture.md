# Architecture — <capability>

Describe the chain (UI → store → API → service → adapter → external system),
the persistence model, the boot sequence, and the auth model. Mirror
`local-agent-terminal/docs/architecture.md`.
